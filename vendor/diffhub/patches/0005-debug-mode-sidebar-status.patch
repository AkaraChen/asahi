Index: b/apps/diffhub/components/DiffsHubHeader.tsx
===================================================================
--- a/apps/diffhub/components/DiffsHubHeader.tsx
+++ b/apps/diffhub/components/DiffsHubHeader.tsx
@@ -72,6 +72,7 @@
   collapseMode: 'expanded' | 'collapsed';
   colorMode: ColorMode;
   darkThemeName: DarkThemeName;
+  debugMode: boolean;
   diffIndicators: DiffIndicators;
   diffStyle: 'split' | 'unified';
   fileTreeAvailable: boolean;
@@ -84,6 +85,7 @@
   onToggleFileTreeOverlay(): void;
   setColorMode(mode: ColorMode): void;
   setDarkThemeName(name: DarkThemeName): void;
+  setDebugMode: Dispatch<SetStateAction<boolean>>;
   setDiffIndicators: Dispatch<SetStateAction<DiffIndicators>>;
   setDiffStyle: Dispatch<SetStateAction<'split' | 'unified'>>;
   setLightThemeName(name: LightThemeName): void;
@@ -98,6 +100,7 @@
   collapseMode,
   colorMode,
   darkThemeName,
+  debugMode,
   diffIndicators,
   diffStyle,
   fileTreeAvailable,
@@ -110,6 +113,7 @@
   onToggleFileTreeOverlay,
   setColorMode,
   setDarkThemeName,
+  setDebugMode,
   setDiffIndicators,
   setDiffStyle,
   setLightThemeName,
@@ -300,6 +304,18 @@
                     />
                   </label>
                 </DropdownMenuItem>
+                <DropdownMenuItem
+                  className="cursor-default p-0"
+                  onSelect={(e) => e.preventDefault()}
+                >
+                  <label className={SETTING_ROW_CLASS}>
+                    <span className="min-w-0 flex-1">Debug mode</span>
+                    <Switch
+                      checked={debugMode}
+                      onCheckedChange={setDebugMode}
+                    />
+                  </label>
+                </DropdownMenuItem>
                 <DropdownMenuItem
                   className="w-full px-2 focus:bg-transparent"
                   onSelect={(e) => e.preventDefault()}
Index: b/apps/diffhub/components/DiffsHubSidebar.tsx
===================================================================
--- a/apps/diffhub/components/DiffsHubSidebar.tsx
+++ b/apps/diffhub/components/DiffsHubSidebar.tsx
@@ -28,7 +28,7 @@
 import { DiffsHubFileTree } from './DiffsHubFileTree';
 import { useChromeThemeProps } from './useChromeThemeProps';
 import type { ThemeCycleControls } from './useThemeCycle';
-import { WorkerPoolStatus } from './WorkerPoolStatus';
+import { PoweredByStatusRow, WorkerPoolStatus } from './WorkerPoolStatus';
 import { Button } from '@/components/Button';
 import { ButtonGroup, ButtonGroupItem } from '@/components/ButtonGroup';
 import {
@@ -60,6 +60,7 @@
 interface DiffsHubSidebarProps {
   className?: string;
   commentSections: readonly DiffsHubSavedCommentItem[];
+  debugMode: boolean;
   diffStats: DiffsHubDiffStatsData | null;
   mobileOverlayOpen?: boolean;
   onMobileClose(): void;
@@ -74,6 +75,7 @@
 export const DiffsHubSidebar = memo(function DiffsHubSidebar({
   className,
   commentSections,
+  debugMode,
   diffStats,
   mobileOverlayOpen = false,
   onMobileClose,
@@ -305,18 +307,24 @@
             />
           </div>
         </div>
-        <DiffsHubDiffStats
-          expanded={activeStatusPanel === 'diffStats'}
-          onToggle={() => toggleStatusPanel('diffStats')}
-          stats={diffStats}
-          streaming={streaming}
-        />
-        <WorkerPoolStatus
-          expanded={activeStatusPanel === 'systemMonitor'}
-          onToggle={() => toggleStatusPanel('systemMonitor')}
-          scrollRef={scrollRef}
-          themeCycle={themeCycle}
-        />
+        {debugMode ? (
+          <>
+            <DiffsHubDiffStats
+              expanded={activeStatusPanel === 'diffStats'}
+              onToggle={() => toggleStatusPanel('diffStats')}
+              stats={diffStats}
+              streaming={streaming}
+            />
+            <WorkerPoolStatus
+              expanded={activeStatusPanel === 'systemMonitor'}
+              onToggle={() => toggleStatusPanel('systemMonitor')}
+              scrollRef={scrollRef}
+              themeCycle={themeCycle}
+            />
+          </>
+        ) : (
+          <PoweredByStatusRow />
+        )}
       </SidebarWrapper>
     </>
   );
Index: b/apps/diffhub/components/ReviewUI.tsx
===================================================================
--- a/apps/diffhub/components/ReviewUI.tsx
+++ b/apps/diffhub/components/ReviewUI.tsx
@@ -63,6 +63,7 @@
   const [showBackgrounds, setShowBackgrounds] = useState(true);
   const [diffIndicators, setDiffIndicators] = useState<DiffIndicators>('bars');
   const [lineNumbers, setLineNumbers] = useState(true);
+  const [debugMode, setDebugMode] = useState(false);
   // All theming state — color mode and the light/dark theme-name picks — lives
   // in the single @pierre/theming controller (the same instance the app-wide
   // ThemeProvider is bound to). Reading it here means picking Auto/Light/Dark
@@ -238,6 +239,7 @@
         collapseMode={collapseMode}
         colorMode={colorMode}
         darkThemeName={darkThemeName}
+        debugMode={debugMode}
         diffIndicators={diffIndicators}
         diffStyle={diffStyle}
         initialUrl={initialUrl}
@@ -250,6 +252,7 @@
         onToggleFileTreeOverlay={handleToggleFileTreeOverlay}
         setColorMode={setColorMode}
         setDarkThemeName={setDarkThemeName}
+        setDebugMode={setDebugMode}
         setDiffIndicators={setDiffIndicators}
         setDiffStyle={setDiffStyle}
         setLightThemeName={setLightThemeName}
@@ -263,6 +266,7 @@
           <DiffsHubSidebar
             className="[grid-area:viewer] md:[grid-area:tree]"
             commentSections={commentSections}
+            debugMode={debugMode}
             diffStats={diffStats}
             mobileOverlayOpen={fileTreeOverlayOpen}
             onMobileClose={handleCloseFileTreeOverlay}
Index: b/apps/diffhub/components/WorkerPoolStatus.tsx
===================================================================
--- a/apps/diffhub/components/WorkerPoolStatus.tsx
+++ b/apps/diffhub/components/WorkerPoolStatus.tsx
@@ -222,32 +222,38 @@
           <StatItem label="Diff Cache" value={stats.diffCacheSize} />
         </div>
       )}
-      <StatusRow icon={IconInfoFill}>
-        <div className="text-muted-foreground/75">
-          Powered by{' '}
-          <Link
-            href="https://diffs.com"
-            target="_blank"
-            rel="noopener noreferrer"
-            className="inline-link text-muted-foreground hover:text-foreground no-underline"
-          >
-            Diffs
-          </Link>{' '}
-          and{' '}
-          <Link
-            href="https://trees.software"
-            target="_blank"
-            rel="noopener noreferrer"
-            className="inline-link text-muted-foreground hover:text-foreground no-underline"
-          >
-            Trees
-          </Link>
-        </div>
-      </StatusRow>
+      <PoweredByStatusRow />
     </div>
   );
 }
 
+export function PoweredByStatusRow() {
+  return (
+    <StatusRow icon={IconInfoFill} className="text-sm">
+      <div className="text-muted-foreground/75">
+        Powered by{' '}
+        <Link
+          href="https://diffs.com"
+          target="_blank"
+          rel="noopener noreferrer"
+          className="inline-link text-muted-foreground hover:text-foreground no-underline"
+        >
+          Diffs
+        </Link>{' '}
+        and{' '}
+        <Link
+          href="https://trees.software"
+          target="_blank"
+          rel="noopener noreferrer"
+          className="inline-link text-muted-foreground hover:text-foreground no-underline"
+        >
+          Trees
+        </Link>
+      </div>
+    </StatusRow>
+  );
+}
+
 interface ThemeCycleToggleProps {
   controls: ThemeCycleControls;
 }

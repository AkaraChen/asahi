Index: b/apps/diffhub/package.json
===================================================================
--- a/apps/diffhub/package.json
+++ b/apps/diffhub/package.json
@@ -27,6 +27,7 @@
     "postcss-import": "catalog:",
     "react": "catalog:",
     "react-dom": "catalog:",
+    "react-grab": "^0.1.47",
     "sonner": "catalog:",
     "tailwind-merge": "catalog:",
     "tailwindcss": "catalog:",
Index: b/apps/diffhub/components/RootLayout.tsx
===================================================================
--- a/apps/diffhub/components/RootLayout.tsx
+++ b/apps/diffhub/components/RootLayout.tsx
@@ -1,5 +1,6 @@
 import { Geist } from 'next/font/google';
 import localFont from 'next/font/local';
+import Script from 'next/script';
 import type { ReactNode } from 'react';
 
 import { PreloadHighlighter } from '@/components/PreloadHighlighter';
@@ -69,6 +70,13 @@
       className={`${berkeleyMono.variable} ${geistSans.variable}`}
     >
       <head>
+        {process.env.NODE_ENV === 'development' ? (
+          <Script
+            src="//unpkg.com/react-grab/dist/index.global.js"
+            crossOrigin="anonymous"
+            strategy="beforeInteractive"
+          />
+        ) : null}
         {/* The iOS navbar tint <meta name="theme-color"> is created and
             managed entirely by the bootstrap script below (and ThemeProvider),
             not authored here — React 19 hoists head tags and would leave a

Index: b/apps/diffhub/components/DiffsHubFileTree.tsx
===================================================================
--- a/apps/diffhub/components/DiffsHubFileTree.tsx
+++ b/apps/diffhub/components/DiffsHubFileTree.tsx
@@ -9,7 +9,6 @@
 import { useFileTree } from '@pierre/trees/react';
 import { type CSSProperties, memo, useEffect, useRef, useState } from 'react';
 
-import type { FileTreePublicId } from '../../../packages/trees/dist/model/publicTypes';
 import { ThemedFileTree } from './ThemedFileTree';
 import {
   BASE_FILE_TREE_OPTIONS,
@@ -17,6 +16,7 @@
   getInitialBatchSize,
 } from '@/lib/constants';
 import type { DiffsHubFileTreeSource } from '@/lib/types';
+type FileTreePublicId = string;
 type FileTreeSortComparator = Exclude<
   NonNullable<FileTreeOptions['sort']>,
   'default'
@@ -119,7 +119,7 @@
         }
       }
       if (source.gitStatusPatch != null) {
-        model.applyGitStatusPatch(source.gitStatusPatch);
+        model.setGitStatus(source.gitStatus);
       }
     } else {
       model.resetPaths(source.paths.slice(0, source.pathCount));
Index: b/apps/diffhub/lib/diffsHubDataAccumulator.ts
===================================================================
--- a/apps/diffhub/lib/diffsHubDataAccumulator.ts
+++ b/apps/diffhub/lib/diffsHubDataAccumulator.ts
@@ -4,7 +4,7 @@
   type FileDiffMetadata,
   parsePatchFiles,
 } from '@pierre/diffs';
-import type { FileTreeGitStatusPatch, GitStatusEntry } from '@pierre/trees';
+import type { GitStatusEntry } from '@pierre/trees';
 
 import { getPatchTreePathPrefix } from './gitPatchMetadata';
 import { mapChangeTypeToGitStatus } from './mapChangeTypeToGitStatus';
@@ -14,6 +14,7 @@
   DiffsHubCommentSidebarFile,
   DiffsHubDiffStats,
   DiffsHubFileTreeSource,
+  FileTreeGitStatusPatch,
 } from './types';
 
 export interface DiffsHubDataAccumulator {
Index: b/apps/diffhub/lib/types.ts
===================================================================
--- a/apps/diffhub/lib/types.ts
+++ b/apps/diffhub/lib/types.ts
@@ -1,5 +1,10 @@
 import type { AnnotationSide, SelectedLineRange } from '@pierre/diffs';
-import type { FileTreeGitStatusPatch, GitStatusEntry } from '@pierre/trees';
+import type { GitStatusEntry } from '@pierre/trees';
+
+export interface FileTreeGitStatusPatch {
+  remove?: readonly string[];
+  set?: readonly GitStatusEntry[];
+}
 
 export type ViewerLoadState =
   | 'fetching'
Index: b/apps/diffhub/next.config.mjs
===================================================================
--- a/apps/diffhub/next.config.mjs
+++ b/apps/diffhub/next.config.mjs
@@ -1,23 +1,3 @@
-import { loadWorktreeEnv } from '../../scripts/load-worktree-env.mjs';
-
-// `next dev` runs under Node, which (like Bun) only auto-loads the standard
-// `.env*` names. Our worktree helper writes `PIERRE_WORKTREE_SLUG` /
-// `PIERRE_PORT_OFFSET` into `.env.worktree` at the worktree root, so pull
-// those in manually before Next inspects `process.env`. moon tasks load the
-// same file via their envFile option; the loader preserves existing values.
-loadWorktreeEnv();
-
-// The browser title prefix (see `app/layout.tsx`) reads
-// `NEXT_PUBLIC_WORKTREE_SLUG` so the value survives into the client bundle.
-// Bridge it from the non-prefixed worktree slug so `.env.worktree` stays the
-// single source of truth.
-if (
-  process.env.PIERRE_WORKTREE_SLUG &&
-  !process.env.NEXT_PUBLIC_WORKTREE_SLUG
-) {
-  process.env.NEXT_PUBLIC_WORKTREE_SLUG = process.env.PIERRE_WORKTREE_SLUG;
-}
-
 /** @type {import('next').NextConfig} */
 const nextConfig = {
   // Strict mode is disabled here to avoid GitHub request thrash in dev: the
@@ -29,14 +9,6 @@
   experimental: {
     cssChunking: 'strict',
   },
-  // Resolve and transpile workspace packages so subpath exports (e.g. @pierre/trees/react)
-  // resolve correctly when Next follows client-component imports from the server.
-  transpilePackages: ['@pierre/trees', '@pierre/diffs', '@pierre/truncate'],
-  turbopack: {
-    resolveAlias: {
-      '@pierre/truncate/style.css': '../../packages/truncate/src/style.css',
-    },
-  },
 };
 
 export default nextConfig;
Index: b/apps/diffhub/package.json
===================================================================
--- a/apps/diffhub/package.json
+++ b/apps/diffhub/package.json
@@ -2,13 +2,19 @@
   "name": "@pierre/diffshub",
   "version": "0.0.0",
   "private": true,
+  "scripts": {
+    "dev": "next dev -p 3692",
+    "build": "next build",
+    "start": "next start -p 3692",
+    "typecheck": "tsc -p tsconfig.json --noEmit"
+  },
   "dependencies": {
-    "@pierre/diffs": "workspace:*",
+    "@pierre/diffs": "1.2.11",
     "@pierre/icons": "catalog:",
-    "@pierre/theme": "workspace:*",
-    "@pierre/theming": "workspace:*",
-    "@pierre/trees": "workspace:*",
-    "@pierre/truncate": "workspace:*",
+    "@pierre/theme": "1.0.3",
+    "@pierre/theming": "0.0.1",
+    "@pierre/trees": "1.0.0-beta.4",
+    "@pierre/truncate": "0.0.2",
     "@radix-ui/react-dropdown-menu": "catalog:",
     "@radix-ui/react-slot": "catalog:",
     "@radix-ui/react-switch": "catalog:",
@@ -28,6 +34,7 @@
   },
   "devDependencies": {
     "@tailwindcss/postcss": "catalog:",
+    "@types/bun": "catalog:",
     "@types/jsdom": "catalog:",
     "@types/node": "catalog:",
     "@types/react": "catalog:",
Index: b/apps/diffhub/tsconfig.json
===================================================================
--- a/apps/diffhub/tsconfig.json
+++ b/apps/diffhub/tsconfig.json
@@ -8,23 +8,6 @@
     "types/**/*.d.ts"
   ],
   "exclude": ["node_modules", ".next"],
-  "references": [
-    {
-      "path": "../../packages/diffs/tsconfig.json"
-    },
-    {
-      "path": "../../packages/trees/tsconfig.json"
-    },
-    {
-      "path": "../../packages/truncate/tsconfig.json"
-    },
-    {
-      "path": "../../packages/theme/tsconfig.json"
-    },
-    {
-      "path": "../../packages/theming/tsconfig.json"
-    }
-  ],
   "compilerOptions": {
     "target": "ES2017",
     "lib": ["dom", "dom.iterable", "esnext"],
